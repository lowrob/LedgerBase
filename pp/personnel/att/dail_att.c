/*-----------------------------------------------------------------------
Source Name: dail_att.c
System     : Personnel/Payroll System.
Created  On: Dec 14, 1991
Created  By: Eugene Roy   

DESCRIPTION:
	The Daily Attendance Screen allows the user to enter the daily 
	attendance received.  All absences for the specified date.

MODIFICATIONS:        

Programmer     YY/MM/DD       Description of modification
------------------------------------------------------------------------*/
#define	MAIN		/* Main program. This is to declare Switches */
#define MAINFL		EMP_ATT			/* main file used */

#define	SYSTEM		"PERSONNEL"		/* Sub System Name */
#define	MOD_DATE	"14-DEC-91"		/* Progran Last Modified */

#include <stdio.h>
#include <cfomstrc.h>
#include <bfs_defs.h>
#include <bfs_pp.h>

/* User Interface define constants */
#ifdef ENGLISH
#define ADDREC		'A'
#define CHANGE		'C'
#define DELETE		'D'
#define INQUIRE		'I'
#define EXITOPT		'E'
#define	NEXT		'N'
#define	PREV		'P'

#define	YES		'Y'
#define NO		'N'
#define ADDITEMS	'A'
#define	LINEEDIT	'L'
#define HEADEREDIT	'H'
#define DELITEM		'D'
#define REACTITEM	'R'
#define	CANCEL		'C'

#define ACTIVE		"ACT"
#define INACTIVE	"DEL"
#else
#define ADDREC		'R'
#define CHANGE		'C'
#define DELETE		'E'
#define INQUIRE		'I'
#define EXITOPT		'F'
#define	NEXT		'S'
#define	PREV		'P'

#define	YES		'O'
#define NO		'N'
#define ADDITEMS	'A'
#define	LINEEDIT	'L'
#define HEADEREDIT	'H'
#define DELITEM		'D'
#define REACTITEM	'R'
#define	CANCEL		'A'

#define ACTIVE		"ACT"
#define INACTIVE	"ELI"
#endif

#define	REACT		99

/* PROFOM Releted declarations */

#define	SCR_NAME	"dail_att"	/* PROFOM screen Name */

#define	PAGESIZE	4		/* No of Items */
/* PROFOM Screen STH file */

/* Field PROFOM numbers */

#define FN_FLD		400	/* Fn: */

#define CHG_FLD		600
#define	DATE_FLD	500	/* CSB/Loan Code */

#define HDR_ST_FLD	1100	/* Desc. Field */

#define	EMP_FLD		1000	/* CSB/Loan Code */
#define NAME_FLD	1100	/* Description field */
#define	CODE_FLD	1200	/* CSB/Loan Code */
#define DESC_FLD	1300	/* Description field */
#define	TEACH_FLD	1500	/* CSB/Loan Code */
#define TNAME_FLD	1600	/* Description field */

#define STATUS_FLD	700	/* Status Field */

#define PAGE_FLD	800	/* Page# Field */

#define	ITEM_ST_FLD	1000	/* Item Start Field */
#define ITEM_END_FLD	4100	/* Item End Field */
#define	STEP		800	/* # of Fields on one line */

#define	END_FLD		4300	/* Last Field of the screen */

/* area.sth - header for C structure generated by PROFOM EDITOR */

typedef	struct	{	/* Start Fld 1500, Endfld 4200 */

	char	s_emp[13];	/* 800 STRING X(6) */
	char	s_name[31];	/* 900 STRING X(30) */
	char	s_code[4];	/* 1100 STRING X(18) */
	char	s_desc[31];	/* 1200 STRING X(18) */
	double	s_hours;	/* hours missed */
	char	s_teach[13];	/* 800 STRING X(6) */
	char	s_tname[30];	/* 900 STRING X(30) */
	char	s_status[5];	/* 1300 STRING XXX */

}	S_item ;

typedef struct	{

	char	s_pgname[11];	/* 100 STRING X(10) */
	long	s_rundate;	/* 300 DATE YYYYFMMFDD */
	char	s_fn[2];	/* 400 STRING X */
	long	s_date;		/* 300 DATE YYYYFMMFDD */
	short	s_field;	/* 500 NUMERIC 99 */
	short	s_page;		/* 700 NUMERIC 99 */
	char	s_stat[4];	/* 400 STRING X */

	S_item	s_items[PAGESIZE] ;	/* Start Fld 800, End Fld 3100  */

	char	s_mesg[78];	/* 3200 STRING X(77) */
	char	s_resp[2];	/* 3300 STRING X */
} s_struct;

static	s_struct  s_sth,image;	/* PROFOM Screen Structure */
static	struct  stat_rec  sr;		/* PROFOM status rec */

static	char 	e_mesg[100];  		/* dbh will return err msg in this */

int	Validation() ;
int	WindowHelp() ;

int	Argc;
char	**Argv;

typedef struct Page {
	S_item	Items[PAGESIZE] ;	/* Items Information */
	struct	Page	*PrevPage ;	/* ptr to previous page */
	struct	Page	*NextPage ;	/* ptr to next page */
	char	I_Status[PAGESIZE][2];	/* item status ie A(DD) C(hange) */
	short	NoItems;		/* number of Items on the page */
	short	Pageno;			/* Page number */
}	Page;

static	Page	*FirstPage,		/* Address of First Page */
		*CurPage,		/* Address of Current Page */
		*CurLast,		/* Address of Curr. record last page */
		*LastPage;		/* Address of Last Page of Memory
					   Allocated */

static	Emp_at_his	att_his,	 pre_att_his;
static	Att	att;
static	Emp	emp_rec;
static	Position	position;
static	Sen_par	sen_par;

static	double	non_sen_days;
static	int	check_dates;

void	free() ;
char	*malloc() ;

main(argc,argv)
int argc;
char *argv[];
{
	int 	retval;

	/* These two are passed to execute() when it is called */
	Argc = argc;
	Argv = argv;

	retval = Initialize(argc,argv);	/* Initialization routine */

	if (retval == NOERROR) retval = Process();

	CloseRtn();			/* return to menu */
	if (retval != NOERROR) exit(-1);
	exit(0);
}

/*-------------------------------------------------------------------*/
/* Initialize PROFOM */

Initialize(argc, argv)
int	argc ;
char	*argv[] ;
{
	int	err ;

	/*
	*	Initialize DBH Environment
	*/
	strncpy(SYS_NAME,SYSTEM,50);	/* Sub system name */
	strncpy(CHNG_DATE,MOD_DATE,10);	/* Modification Date */

	proc_switch(argc, argv, MAINFL) ; 	/* Process Switches */

	/*
	*	Initialize PROFOM & Screen
	*/
	strcpy(sr.termnm,terminal);	/* Copy Terminal Name */
	fomin(&sr);
	ret(err_chk(&sr)) ;		/* Check for PROFOM Error */
	fomcf(1,1);			/* Enable Snap screen option */

	err = InitScreen() ;		/* Initialize Screen */
	if(NOERROR != err) return(err) ;

	FirstPage = NULL;
	LastPage = NULL;

	return(NOERROR) ;
}	/* Initialize() */

/*--------------------------------------------------------------------------*/
/* Close nessary files and environment before exiting program               */

CloseRtn() 
{
	/* Free the linked list for the end */
	for( ;LastPage != FirstPage ; ) {
		LastPage = LastPage->PrevPage;
		free((char *)LastPage->NextPage);
		LastPage->NextPage = NULL;
	}
	if(FirstPage != NULL) {
		free((char *)FirstPage);
	}

	FirstPage = LastPage = NULL;

	/* Set terminal back to normal mode from PROFOM */
	fomcs();
	fomrt();

	close_dbh();	/* Close files */

	return(NOERROR);
}	/* CloseRtn() */
/*----------------------------------------------------------------*/
/* Initialize screen before going to process options */

InitScreen()
{
	int	err ;

	/* move screen name to Profom status structure */
	strcpy(sr.scrnam,NFM_PATH);
	strcat(sr.scrnam,SCR_NAME) ;

	strcpy(s_sth.s_pgname,PROG_NAME);

	s_sth.s_rundate = get_date();	/* get Today's Date in YYYYMMDD format*/
	s_sth.s_field = HV_SHORT ;
	s_sth.s_mesg[0] = HV_CHAR ;
	s_sth.s_resp[0] = HV_CHAR ;

	/* Move High Values to data fields and Display the screen */
	err = ClearScreen() ;
	if(NOERROR != err) return(err) ;

	return(NOERROR) ;
}	/* InitScreen() */
/*-------------------------------------------------------------------*/
/* Get Option from user and call corresponding function */

Process()
{
	int err;

	for( ; ; ){

		/* Get the Fn: option from the user */
		if((err = ReadFunction()) != NOERROR) return(err) ;

		err = ProcFunction() ;	/* Process Function */

		if(QUIT == err)		return(NOERROR) ;    /* Exit */
		if(NOACCESS == err)	fomen(e_mesg);	     /* security */
		if(PROFOM_ERR == err)	return(PROFOM_ERR);  /* PROFOM ERROR */
		if(DBH_ERR == err) {
			DispError((char *)&s_sth,e_mesg);
#ifdef ENGLISH
			sprintf(e_mesg,"%s %d Dberror: %d Errno: %d",
				"System Error... Iserror:",
				iserror, dberror, errno);
#else
			sprintf(e_mesg,"%s %d Dberror: %d Errno: %d",
				"Erreur du systeme... Iserror:",
				iserror, dberror, errno);
#endif
			DispError((char *)&s_sth,e_mesg);
			return(DBH_ERR); /* DBH ERROR */
		}
	}      /*   end of the for( ; ; )       */
}	/* Process() */
/*----------------------------------------------------------------*/
/* Display the Function (Fn:) options and get the option from the user */

ReadFunction()
{
	/* Display Fn: options */
#ifdef ENGLISH
	fomer("A(dd), I(nquire), E(xit)");
#else
	fomer("R(ajouter), I(nteroger), F(in)");
#endif
	/* Read Fn: field to get the option */
	sr.nextfld = FN_FLD ;
	fomrf( (char *)&s_sth );
	ret(err_chk(&sr));	/* Check for PROFOM error */

	return(NOERROR) ;
}	/* ReadFunction() */
/*----------------------------------------------------------------*/
/* Process the user selected Fn: option */

ProcFunction()
{
	int retval;

	switch (s_sth.s_fn[0]) {
	case ADDREC  :			/* ADD */
		CHKACC(retval,ADD,e_mesg);
		return( Add() ) ;
	case INQUIRE  :			/* ADD */
		CHKACC(retval,ADD,e_mesg);
		return( Inquire() ) ;
	case EXITOPT  :			/* Exit */
		return( QUIT ) ;
	}  /*   end of the switch statement */

	return(retval);
}	/* ProcFunction() */
/*----------------------------------------------------------------------*/
/* Adding.  Get the unique Key, accept details and update the files */
Add()
{
	int	err ;

	FirstPage = NULL;
	LastPage = NULL;
	CurLast = NULL;

	err = ReadKey();
	if(err != NOERROR) return(err) ;

	/* Clear The Screen */
	err = ClearScreen();
	if(err != NOERROR) return(err) ;

	err = GetDetails() ;
	if(PROFOM_ERR == err || DBH_ERR == err) return(err) ;
	if(err < 0 || CANCEL == err) {
		roll_back(e_mesg) ;	/* Unlock the locked Records */
		return(ClearScreen()) ;	/* Clear the Screen */
	}

	return(NOERROR);
}	/* Add() */
/*----------------------------------------------------------------------*/
Inquire()
{
	int	err ;

	FirstPage = NULL;
	LastPage = NULL;
	CurLast = NULL;

	err = SelectRecord();
	if(err != NOERROR) return(err) ;

	err = ConfirmItems() ;

	return(NOERROR);
}	/* Add() */
/*----------------------------------------------------------------------*/
/* Get the Student Study Hall key from user. In ADD mode disable dup buffers, */
/* other modes enable dup buffers and show the current key as a default key */
/*----------------------------------------------------------------------*/
ReadKey()
{
	int	i;
	
	/* In Add mode turn off dup control for key fields.
	   Other modes reverse it */
	if(s_sth.s_fn[0] == ADDREC){	/* ADD */
		SetDupBuffers(DATE_FLD,DATE_FLD,0); /* Off Dup Control */
	}
	else {
		SetDupBuffers(DATE_FLD,DATE_FLD,1);
	}

#ifdef ENGLISH
	strcpy(s_sth.s_mesg,"Press ESC-F to Go Back to Fn:");
#else
	strcpy(s_sth.s_mesg,"Appuyer sur ESC-F pour retourner a Fn:");
#endif
	DispMesgFld((char *)&s_sth);

	s_sth.s_date = LV_LONG;

	i = ReadFields((char *)&s_sth,DATE_FLD, DATE_FLD,
		Validation, WindowHelp,1) ;
	if(PROFOM_ERR == i || DBH_ERR == i) return(i) ;
	if(RET_USER_ESC == i){
		
		ret( WriteFields((char *)&s_sth,DATE_FLD, DATE_FLD) ) ;

		s_sth.s_mesg[0] = HV_CHAR;
		DispMesgFld((char *)&s_sth);

		return(RET_USER_ESC) ;
	}

	s_sth.s_mesg[0] = HV_CHAR;
	DispMesgFld((char *)&s_sth);

	return(NOERROR);
}	/*  ReadKey() */
/*------------------------------------------------------------*/
/* Read the Area Details from the User */

GetDetails()
{
	int	i ;

	i = AddItems();
	if(NOERROR != i) return(i) ;

	for( ; ; ) {
		i = ConfirmItems() ;
		if(i != YES) break;

		i = ProcItemUpdates(ADD) ;
		if(i < 0) {
			if(i == LOCKED) continue;
			break;
		}
		break;
	}
	if(i != NOERROR) return(i);
	return(NOERROR) ;
}	/* GetDetails() */
/*------------------------------------------------------------*/
/* Read Item Details from the User */

AddItems()
{
	int	i, err ;

	/* If the last node of po is Partial filled then Show Page */
	if(CurLast != NULL && CurLast->NoItems < PAGESIZE ) {
		ret( ShowItems(CurLast) ) ;
		i = CurLast->NoItems ;
		CurPage = CurLast ;
	}
	else {
		/* Calculate the page# */
		if(CurLast != NULL) {
			i = PAGESIZE ;
			CurPage = CurLast ;
		}
		else {
			s_sth.s_page = 1 ;
			ret( WriteFields((char *)&s_sth,PAGE_FLD,PAGE_FLD+100));
			i = 0 ;
		}
	}


	for( ; ; ) {
		if( PAGESIZE == i) {	/* Page Full */

			/* Move High Values to All items except first */
			for(i-- ; i > 0 ; i--)
				InitItem(HV_CHAR, HV_DOUBLE, i) ;

			/* Calculate the page# */
			s_sth.s_page = CurLast->Pageno + 1 ;

			ret( WriteFields((char *)&s_sth,PAGE_FLD, 
				(END_FLD - 200)) ) ;

			i = 0 ;
		}

		err = ReadItem(i,ADD) ;		/* Read Each Item Line */
		if(PROFOM_ERR == err || DBH_ERR == err) return(err) ;
		if(NOERROR != err) break ;	/* ESC-F */

		if(0 == i)	/* First Item in the Page */
			if((err = MakeFreshPage()) < 0) return(err) ;
		
		/* Copy the Item to List */
		scpy((char*)&(CurPage->Items[i]), (char*)&(s_sth.s_items[i]),
			sizeof(S_item)) ;

		CurPage->I_Status[i][0] = ADDITEMS;

		i++ ;

		CurPage->NoItems = i;
	}
	if(i == 0) 
		if((err=ShowItems(CurPage))<0) return(err) ;

	return(NOERROR) ;
}	/* AddItems() */
/*-----------------------------------------------------------------------*/
/*	Get the next node in linked list to add invoice items. If the
*	(Cur. invc last page) = (Last Page in linked List) or no
*	nodes in list, allocate node and add to linked list
*/

MakeFreshPage()
{
	Page	*tempptr ;

	/* If, no node is allocated yet or Current invoice used all the nodes,
	   then allocate new node */

	if( LastPage == NULL || CurLast == LastPage ){
		tempptr= (Page *)malloc((unsigned)sizeof(Page)) ;

		if( tempptr == NULL ){
			DispError((char*)&s_sth,"Memory Allocation Error");
			return(ERROR);
		}
		tempptr->NextPage = NULL ;

		if( LastPage == NULL ){	/* No node is allocated Yet */
			tempptr->PrevPage = NULL ;
			tempptr->Pageno = 1 ;
			FirstPage = tempptr ;
		}
		else {				/* Not a first node in list */
			tempptr->Pageno = LastPage->Pageno + 1 ;
			LastPage->NextPage = tempptr ;
			tempptr->PrevPage = LastPage ;
		}
		LastPage = tempptr ;
	}

	if(CurLast == NULL)
		CurLast = FirstPage ;
	else
		CurLast = CurLast->NextPage ;

	CurLast->NoItems = 0 ;
	CurPage = CurLast ;

	return(NOERROR);
}	/* MakeFreshPage() */
/*----------------------------------------------------------*/
/* Get the key and show the record */
SelectRecord()
{
	int	err ;

	err = ReadKey();
	if(err != NOERROR) return(err) ;

	err = ShowScreen();
	if(err < 0) {
		return(err);
	}

	return(NOERROR);
}	/* SelectRecord() */
/*------------------------------------------------------------*/
ShowScreen()
{
	int	err ;

	/* Get area records i.e. Build list */
	err = BuildList();
	if(err < 0 && err != EFL) {
		return(err);
	}

	err = ShowItems(CurPage);
	if(err < 0) {
		return(err);
	}
	
	return(NOERROR);
}
/*------------------------------------------------------------*/
BuildList()
{
	int retval;
	int i;
	int j;
	CurLast = CurPage = NULL;
	i = 0;

	att_his.eah_numb[0] = '\0';	
	att_his.eah_date = 0;	
	flg_reset(EMP_ATT);

	for( ; ; ) {
		retval = get_n_emp_at(&att_his,BROWSE,0,FORWARD,e_mesg);
		if(retval < 0) {
			if(retval == EFL) break;
			DispError((char *)&s_sth,e_mesg);
			return(retval);
		}
		if(att_his.eah_date != s_sth.s_date)	continue;

		if(PAGESIZE == i) i = 0;
		if(i == 0) {
			if((retval = MakeFreshPage()) < 0) return(retval);
		}
		strcpy(emp_rec.em_numb, att_his.eah_numb);

		retval = get_employee(&emp_rec,BROWSE,0,e_mesg);
		if(retval < 0) {
			fomer("Employee Number Does not Exist");
		}

		retval = UsrBargVal(BROWSE,emp_rec.em_numb,emp_rec.em_barg,0,e_mesg);
		if(retval < 0){
			if(retval == ERROR) continue;
			DispError((char *)&s_sth,e_mesg);
			return(ERROR);
		}
		strcpy(CurPage->Items[i].s_emp, att_his.eah_numb);
		strcpy(e_mesg,emp_rec.em_first_name);
		strcat(e_mesg," ");
		strcat(e_mesg,emp_rec.em_last_name);
		strncpy(CurPage->Items[i].s_name,e_mesg,28);

		strcpy(CurPage->Items[i].s_code, att_his.eah_code);

		strcpy(att.at_code, att_his.eah_code);

		retval = get_att(&att,BROWSE,1,e_mesg);
		if(retval < 0) {
			fomer("Invalid Attendance Code");
		}

		CurPage->Items[i].s_hours = att_his.eah_hours;
		strcpy(CurPage->Items[i].s_desc, att.at_desc);
		if(att_his.eah_teach[0] != '\0' && 
		     strcmp(att_his.eah_teach,"            ")!=0 &&
		     att_his.eah_teach[0] != HV_CHAR){
		  strcpy(CurPage->Items[i].s_teach, att_his.eah_teach);
		  strcpy(emp_rec.em_numb, att_his.eah_teach);

		  retval = get_employee(&emp_rec,BROWSE,0,e_mesg);
		  if(retval < 0) {
			fomer("Employee Number Does not Exist");
		  }
		  else{
		  	strcpy(e_mesg,emp_rec.em_first_name);
		  	strcat(e_mesg," ");
		  	strcat(e_mesg,emp_rec.em_last_name);
		  	strncpy(CurPage->Items[i].s_tname,e_mesg,28);
		  }
		}
		else{
		  CurPage->Items[i].s_teach[0] = HV_CHAR;
		  CurPage->Items[i].s_tname[0] = HV_CHAR;
		}

		strcpy(CurPage->Items[i].s_status,ACTIVE);

		CurPage->I_Status[i][0] = ' ';
		CurPage->NoItems++;
		i++;
	}
	seq_over(EMP_ATT);

	if(CurLast != NULL) {
		CurPage = FirstPage;
	}

	if(retval == EFL) return(retval);
	return(NOERROR);
}
/*------------------------------------------------------------*/
/* Show all the items on the current page 		      */
ShowItems(pageptr)
Page	*pageptr ;
{
	int	i ;


	if(pageptr != NULL) {
		/* Copy the items to screen */
		scpy((char*)s_sth.s_items, (char*)pageptr->Items,
			(pageptr->NoItems * sizeof(S_item)) );

		s_sth.s_page   = pageptr->Pageno ;
		i = pageptr->NoItems ;
	}
	else {
		s_sth.s_page = HV_SHORT ;
		i = 0 ;
	}

	/* Move High Values to remaining Items */
	for( ; i < PAGESIZE ; i++ )
		InitItem(HV_CHAR,HV_DOUBLE,i);

	ret( WriteFields((char *)&s_sth, PAGE_FLD, (END_FLD - 200)) );

	return(NOERROR) ;
}	/* ShowItems() */
/*-----------------------------------------------------------------------*/ 
/* Process all the items in the link list and write any changes to the   */
/* file.								 */
/*-----------------------------------------------------------------------*/ 
ProcItemUpdates(mode)
int	mode;
{
	Page	*temppage;
	int	i;
	int	retval;
	int	write_mode;

	scpy((char *)&pre_att_his,(char *)&att_his,sizeof(att_his));
	
	if(CurLast != NULL) {
	   for(temppage=FirstPage; temppage!=NULL;temppage=temppage->NextPage) {
	      for(i =0; i< temppage->NoItems; i++) {
		 retval = GetMode(temppage,i,mode,&write_mode);
		 if(write_mode == NOOP) continue;
		 retval = WriteRecords(temppage,i,write_mode);
  		 if(retval < 0) {
			if(retval == LOCKED) return(LOCKED);
		   	break;
		 }
	      }
	      if(temppage == CurLast) break;
	   }
	}
	return(NOERROR);
}
/*-----------------------------------------------------------------------*/ 
GetMode(temppage,item_no,mode,write_mode)
Page	*temppage;
int	item_no;
int	mode;
int	*write_mode;
{
	if(mode == ADD) {
		if(strcmp(temppage->Items[item_no].s_status,INACTIVE)==0) {
			*write_mode = NOOP;
		}
		else {
			*write_mode = ADD;
		}
	}
	else if(mode == UPDATE) {
		if(strcmp(temppage->Items[item_no].s_status,INACTIVE)==0) {
			*write_mode = P_DEL;
		}
		else if(temppage->I_Status[item_no][0] == ADDITEMS) {
			*write_mode = ADD;
		}
		else if(temppage->I_Status[item_no][0] == CHANGE) {
			*write_mode = UPDATE;
		}
		else if(temppage->I_Status[item_no][0] == ' ') {
			*write_mode = NOOP;
		}
	}
	else if(mode == P_DEL) {
		*write_mode = P_DEL;
	}

	return(NOERROR);
}
/*-----------------------------------------------------------------------*/ 
/* Write the Loan record to the file.			 		 */ 
/*-----------------------------------------------------------------------*/ 
WriteRecords(temppage,item_no,mode)
Page	*temppage;
int	item_no;
int	mode;
{
	int	retval;
	int	i;

	scpy((char *)&pre_att_his,(char *)&att_his,sizeof(att_his));

	strcpy(emp_rec.em_numb,temppage->Items[item_no].s_emp);
	retval = get_employee(&emp_rec,BROWSE,0,e_mesg);
	if(retval < 0) {
		DispError((char *)&s_sth,e_mesg);
		return(retval);
	}

	strcpy(sen_par.sn_position,emp_rec.em_pos);
	sen_par.sn_eff_date = get_date();
	flg_reset(SEN_PAR);

	retval = get_n_sen_par(&sen_par, BROWSE, 0, BACKWARD, e_mesg);
	if(retval < 0) {
  		DispError((char *)&s_sth,e_mesg);
		return(NOERROR);
	}
	seq_over(SEN_PAR);

	if(strcmp(sen_par.sn_position,emp_rec.em_pos)!=0){
		DispError((char *)&s_sth,"Seniority Parameter Record Not on File");
		return(NOERROR);
	}
	strcpy(att.at_code,temppage->Items[item_no].s_code);

	retval = get_att(&att,BROWSE,1,e_mesg);
	if(retval < 0) {
		DispError((char *)&s_sth,e_mesg);
		return(retval);
	}

	strcpy(att_his.eah_numb,temppage->Items[item_no].s_emp);
	if(mode != ADD) {
		retval = get_emp_at(&att_his,UPDATE,0,e_mesg) ;
		if(retval < 0) {
			DispError((char *)&s_sth,e_mesg);
			roll_back(e_mesg);
			return(retval);
		}
	}
	else{
		att_his.eah_sen_hours = sen_par.sn_num_hrs_day;	
		att_his.eah_vacproc[0] = NULL;
	}
	strcpy(att_his.eah_code,temppage->Items[item_no].s_code);
	att_his.eah_date = s_sth.s_date;
	att_his.eah_hours = temppage->Items[item_no].s_hours;
	strcpy(att_his.eah_teach,temppage->Items[item_no].s_teach);
	if(att_his.eah_teach[0] == HV_CHAR)
		att_his.eah_teach[0] = '\0';

	retval = put_emp_at(&att_his,mode,e_mesg) ;
	if(retval < 0) {
		DispError((char *)&s_sth,e_mesg);
		roll_back(e_mesg);
		return(retval);
	}

	if(mode != ADD) {
		retval = rite_audit((char*)&s_sth,EMP_ATT,mode,(char*)&att_his,
			(char*)&pre_att_his,e_mesg);
		if(retval==LOCKED) {
			DispError((char *)&s_sth,e_mesg);
			roll_back(e_mesg) ;
			return(LOCKED) ;
		}
		if(retval < 0 ){
			DispError((char *)&s_sth,"ERROR: Saving Records"); 
			DispError((char *)&s_sth,e_mesg);
			roll_back(e_mesg);
			return(retval);
		}
	}

	retval = commit(e_mesg) ;
	if(retval < 0) {
		DispError((char *)&s_sth,"ERROR: Saving Records"); 
		DispError((char *)&s_sth,e_mesg);
		roll_back(e_mesg);
		return(retval);
	}

	return(NOERROR);
}
/*------------------------------------------------------------*/
/* Read details of given item# */
/*------------------------------------------------------------*/
ReadItem(item_no,mode)
int	item_no ;
int	mode ;
{
	int	i;
	int	st_fld ;
	int	end_fld ;

	if(mode == ADD) {
		SetDupBuffers(ITEM_ST_FLD, END_FLD - 200,0);
	}
	else {
		SetDupBuffers(ITEM_ST_FLD, END_FLD - 200,1);
	
	}

#ifdef ENGLISH
	strcpy(s_sth.s_mesg,"Press ESC-F to Terminate");
#else
	strcpy(s_sth.s_mesg,"Appuyer sur ESC-F pour terminer");
#endif
	DispMesgFld((char *)&s_sth);

	st_fld  = ITEM_ST_FLD + (STEP * item_no) + 0;
	end_fld  = ITEM_ST_FLD + (STEP * item_no) + (STEP - 200);
	if(mode == ADD) {
		strcpy(s_sth.s_items[item_no].s_status,ACTIVE);
	}
	InitItem(LV_CHAR,LV_DOUBLE,item_no);
	i = ReadFields((char *)&s_sth,st_fld,end_fld,Validation,WindowHelp,1) ;
	if(PROFOM_ERR == i || DBH_ERR == i) return(i) ;
	if(RET_USER_ESC == i) {
		if(mode == ADD) {
			InitItem(HV_CHAR,HV_DOUBLE,item_no);
			WriteFields((char *)&s_sth,ITEM_ST_FLD,END_FLD);
			return(RET_USER_ESC);
		}
		/* When user gives ESC-F while changing fields, assumption is
		* he completed his changes, and remaining fields are same as
		* old. But, at this point STH will be having low values in the 
		* remaining fields. So move the old values form the linked list.
		*/

		i = CopyBack((char *)&s_sth,(char *)&image,sr.curfld, END_FLD);
		if(i == PROFOM_ERR) return(i);

		return(RET_USER_ESC) ;
	}

	return(NOERROR) ;
}	/* ReadItem() */
/*----------------------------------------------------------------*/
/* Set Duplication buffers for fields 				  */
SetDupBuffers( firstfld, lastfld, value )
int	firstfld, lastfld;	/* field numbers range */
int	value;			/* ENABLE or DISABLE */
{
	int i ;

	for( i=firstfld; i<=lastfld; i+=100 )
		fomca1( i, 19, value);
	if( value==0 )
		return(0);
	sr.nextfld = firstfld;
	sr.endfld = lastfld;
	fomud( (char *)&s_sth );
	ret( err_chk(&sr) );

	return( 0 );
}
/*----------------------------------------------------------------*/
/* Validation function() for Key and Header fields when PROFOM returns
  RET_VAL_CHK */

Validation()
{
	int	retval, i, item_no, fld_no, remain;
	long	julian;

	item_no = (sr.curfld - ITEM_ST_FLD) / STEP;
	fld_no = sr.curfld - (item_no * STEP);

	switch(fld_no){
	case EMP_FLD:
		Right_Justify_Numeric(s_sth.s_items[item_no].s_emp,
			sizeof(s_sth.s_items[item_no].s_emp)-1);
		strcpy(emp_rec.em_numb,s_sth.s_items[item_no].s_emp);

		retval = get_employee(&emp_rec,BROWSE,0,e_mesg);
		if(retval < 0) {
			fomer("Employee Number Does not Exist");
			s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(ERROR);
		}

		retval = UsrBargVal(UPDATE,emp_rec.em_numb,emp_rec.em_barg,1,e_mesg);
		if(retval < 0){
			DispError((char *)&s_sth,e_mesg);
		  	s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(-1);
		}
/*	  	strcpy(position.p_code,emp_rec.em_pos);
	  	retval = get_position(&position,BROWSE,0,e_mesg);

	 	if((strcmp(position.p_type, "FT") !=0) &&
				 (strcmp(position.p_type, "PT")!= 0)){
			fomer("Employee is not Permanent");
			s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(ERROR);
		} */
		att_his.eah_date = s_sth.s_date;
		strcpy(att_his.eah_numb,s_sth.s_items[item_no].s_emp);
		retval = get_emp_at(&att_his,BROWSE,0,e_mesg);
		if(retval < 0 && retval != UNDEF) {
			DispError((char *)&s_sth,e_mesg);
			s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(ERROR);
		}	
		if(retval != UNDEF) {
			fomer("Attendance Already Exists");
			s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(ERROR);
		}
		retval = CheckDate(item_no);
		if(retval < 0) {
			s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(ERROR);
		}	
		strcpy(e_mesg,emp_rec.em_first_name);
		strcat(e_mesg," ");
		strcat(e_mesg,emp_rec.em_last_name);
		strncpy(s_sth.s_items[item_no].s_name, e_mesg,31);

		break;

	case DATE_FLD:
		if(s_sth.s_date == 0) {
			fomer("Invalid Date");
			s_sth.s_date = LV_LONG;
			return(ERROR);
		}
		julian = days(s_sth.s_date);
		remain = julian % 7;

		if(remain == 6 || remain == 0){
			DispError((char *)&s_sth,"Day Selected is a Weekend");
		}
		break;
	case CODE_FLD: 
		Right_Justify_Numeric(s_sth.s_items[item_no].s_code,
			sizeof(s_sth.s_items[item_no].s_code)-1);
		strcpy(att.at_code,s_sth.s_items[item_no].s_code);

		retval = get_att(&att,BROWSE,1,e_mesg);
		if(retval < 0) {
			fomer("Invalid Attendance Code");
			s_sth.s_items[item_no].s_code[0] = LV_CHAR;
			return(ERROR);
		}
		strcpy(s_sth.s_items[item_no].s_desc, att.at_desc);
		break;
	case TEACH_FLD:
		if(s_sth.s_items[item_no].s_teach[0] == '\0'){
			s_sth.s_items[item_no].s_teach[0] = HV_CHAR;
			s_sth.s_items[item_no].s_tname[0] = HV_CHAR;
			sr.curfld += 200;
			break;
		}
		Right_Justify_Numeric(s_sth.s_items[item_no].s_teach,
			sizeof(s_sth.s_items[item_no].s_teach)-1);
		strcpy(emp_rec.em_numb,s_sth.s_items[item_no].s_teach);

		retval = get_employee(&emp_rec,BROWSE,0,e_mesg);
		if(retval < 0) {
			fomer("Employee Number Does Not Exist");
			s_sth.s_items[item_no].s_teach[0] = LV_CHAR;
			return(ERROR);
		}

		retval = UsrBargVal(UPDATE,emp_rec.em_numb,emp_rec.em_barg,1,e_mesg);
		if(retval < 0){
			DispError((char *)&s_sth,e_mesg);
		  	s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(-1);
		}
		strcpy(e_mesg,emp_rec.em_first_name);
		strcat(e_mesg," ");
		strcat(e_mesg,emp_rec.em_last_name);
		strncpy(s_sth.s_items[item_no].s_tname, e_mesg,31);

		break;
	default:
#ifdef ENGLISH
		sprintf(e_mesg,"No Validity Check for Field#  %d",sr.curfld);
		
#else
		sprintf(e_mesg,"Pas de controle de validite pour le Champ#  %d",sr.curfld);
#endif
		fomen(e_mesg);
		get();
		return(ERROR) ;
	}	/* Switch sr.curfld */

	return(NOERROR) ;
}	/* Validation() */
/*----------------------------------------------------------------*/
/* Show Help Windows, if applicable, when user gives ESC-H in key
   and header fields */

WindowHelp()
{
	int	retval, i, item_no, fld_no, remain ;
	short	reccod;
	long	julian;

	item_no = (sr.curfld - ITEM_ST_FLD) / STEP;
	fld_no = sr.curfld - (item_no * STEP);

	switch(fld_no){
	case EMP_FLD:
		retval = emp_hlp(s_sth.s_items[item_no].s_emp,7,13);
		if(retval == DBH_ERR) return(retval);
		redraw();
		Right_Justify_Numeric(s_sth.s_items[item_no].s_emp,
			sizeof(s_sth.s_items[item_no].s_emp)-1);
		strcpy(emp_rec.em_numb,s_sth.s_items[item_no].s_emp);
		retval = get_employee(&emp_rec,BROWSE,0,e_mesg);
		if(retval < 0) {
			fomer("Employee Number Does not Exist");
			s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(ERROR);
		}
	 /* 	strcpy(position.p_code,emp_rec.em_pos);
	  	retval = get_position(&position,BROWSE,0,e_mesg);

	 	if((strcmp(position.p_type, "FT") !=0) &&
				 (strcmp(position.p_type, "PT")!= 0)){
			fomer("Employee is not Permanent");
			s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(ERROR);
		} */
		/* allow NFLD to enter codes for weekends */
		check_dates = 0;

		if(check_dates == 1){
			julian = days(s_sth.s_date);
			remain = julian % 7;

			if(remain == 6 || remain == 0){

				fomer("Cannot Enter an Attendance Code for a Weekend Date for This Employee");
				s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
				return(ERROR);
			}
		}
		att_his.eah_date = s_sth.s_date;
		strcpy(att_his.eah_numb,s_sth.s_items[item_no].s_emp);
		retval = get_emp_at(&att_his,BROWSE,0,e_mesg);
		if(retval < 0 && retval != UNDEF) {
			DispError((char *)&s_sth,e_mesg);
			s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(ERROR);
		}	
		if(retval != UNDEF) {
			fomer("Attendance Already Exists");
			s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(ERROR);
		}
		retval = CheckDate(item_no);
		if(retval < 0) {
			s_sth.s_items[item_no].s_emp[0] = LV_CHAR;
			return(ERROR);
		}	

		strcpy(e_mesg,emp_rec.em_first_name);
		strcat(e_mesg," ");
		strcat(e_mesg,emp_rec.em_last_name);
		strncpy(s_sth.s_items[item_no].s_name, e_mesg,31);

		break;

	case CODE_FLD:
		retval = att_hlp(s_sth.s_items[item_no].s_code,7,13);
		if(retval == DBH_ERR) return(retval);
		if(retval >= 0) redraw();
		if(retval == 0) return(ERROR);
		if(retval < 0) return(ERROR);
		strcpy(att.at_code,s_sth.s_items[item_no].s_code);
		retval = get_att(&att,BROWSE,1,e_mesg);
		if(retval < 0) {
			fomer(e_mesg);
			s_sth.s_items[item_no].s_code[0] = LV_CHAR;
			return(ERROR);
		}
		strcpy(s_sth.s_items[item_no].s_desc, att.at_desc);
		break;
	case TEACH_FLD:
		retval = emp_hlp(s_sth.s_items[item_no].s_teach,7,13);
		if(retval == DBH_ERR) return(retval);
		redraw();
		Right_Justify_Numeric(s_sth.s_items[item_no].s_teach,
			sizeof(s_sth.s_items[item_no].s_teach)-1);
		strcpy(emp_rec.em_numb,s_sth.s_items[item_no].s_teach);
		retval = get_employee(&emp_rec,BROWSE,0,e_mesg);
		if(retval < 0) {
			fomer("Employee Number Does not Exist");
			s_sth.s_items[item_no].s_teach[0] = LV_CHAR;
			return(ERROR);
		}

		strcpy(e_mesg,emp_rec.em_first_name);
		strcat(e_mesg," ");
		strcat(e_mesg,emp_rec.em_last_name);
		strncpy(s_sth.s_items[item_no].s_tname, e_mesg,31);

		break;

	default :
		fomer("No Help Window for This Field");
	}	/* Switch sr.curfld */

	return(NOERROR) ;
}	/* HdrAndKeyWindowHelp() */
/*-----------------------------------------------------------------------*/
static	
CheckDate(item_no)
int	item_no;

{
	int	retval;
	int	i;
	Page	*temppage;
	
	att_his.eah_date = s_sth.s_date;
	strcpy(att_his.eah_numb,s_sth.s_items[item_no].s_emp);

	retval = get_emp_at(&att_his,BROWSE,0,e_mesg);
	if(retval < 0 && retval != UNDEF) {
		DispError((char *)&s_sth,e_mesg);
		roll_back(e_mesg);
		return(retval);
	}
	if(retval != UNDEF) {
		fomer("Attendance Record Already Exists on File");
		return(-1);
	}
	/* check to see if item already exists on list */
	if(CurLast != NULL) {
	   for(temppage=FirstPage; temppage!=NULL;temppage=temppage->NextPage) {
	      for(i =0; i< temppage->NoItems; i++) {
		 	if(temppage->Items[i].s_code[0] == '\0')
				continue;
			if(strcmp(temppage->Items[i].s_emp,
				s_sth.s_items[item_no].s_emp)==0) {
					fomer("Employee's Attendance Code Already Exists");
					return(ERROR);
			}
	      }
	      if(temppage == CurLast) break;
	   }
	}
	return(NOERROR);
}
/*-----------------------------------------------------------------------*/
/* Take the confirmation from user for the items part of the screen      */
/*-----------------------------------------------------------------------*/
ConfirmItems()
{
	int	err ;

	/* Options:
	     YLNPC
	*/

	for( ; ; ) {
	    if(s_sth.s_fn[0] == CHANGE) {
#ifdef ENGLISH
			err = GetOption((char *)&s_sth,
		"Y(es), S(creen edit), L(ine), D(el), R(eactivate), N(ext), P(rev), C(ancel)"
		,"YAHLDRNPC");
#else
			err = GetOption((char *)&s_sth,
		"O(ui), S(creen edit), L(ine edit), S(uiv), P(rec), A(nnul)"
		,"OSLA");
#endif
	    }
	    else if(s_sth.s_fn[0] != INQUIRE) {
#ifdef ENGLISH
			err = GetOption((char *)&s_sth,
		"Y(es), A(dd), D(el), R(eactivate), N(ext), P(rev), C(ancel)"
		,"YADRNPC");
#else
			err = GetOption((char *)&s_sth,
		"O(ui), R(ajouter), E(liminer), V(ivifier), S(uivant), P(recedent), A(nnuler)"
		,"OREVSPA");
#endif
	    }
	    else {
#ifdef ENGLISH
			err = GetOption((char *)&s_sth,
		"Y(es), N(ext), P(rev)","YNP");
#else
			err = GetOption((char *)&s_sth,
		"O(ui), S(uiv), P(rec)" ,"OSP");
#endif
	    }
	    switch(err) {
	    case  YES  :
		return(YES);
	    case  ADDITEMS:
		err = AddItems();
		break;
	    case  DELITEM:
	    case  REACTITEM:
		err = ChangeStatus(err);
		break;
	    case  NEXT:
		if(CurPage == CurLast || CurLast == NULL) {
#ifdef ENGLISH
			fomer("No More Pages....");
#else
			fomer("Plus de pages....");
#endif
			continue;
		}
		CurPage = CurPage->NextPage ;
		err = ShowItems(CurPage);
		break;
	    case  PREV:
		if(CurLast == NULL || CurPage == FirstPage) {
#ifdef ENGLISH
			fomer("No More Pages....");
#else
			fomer("Plus de pages....");
#endif
			continue;
		}
		CurPage = CurPage->PrevPage ;
		err = ShowItems(CurPage);
		break;
	    case  CANCEL :
#ifdef ENGLISH
		err = GetOption((char *)&s_sth,
				"Confirm the Cancel (Y/N)?", "YN") ;
#else
		err = GetOption((char *)&s_sth,
				"Confirmer l'annulation (O/N)?", "ON") ;
#endif
		if(err == YES) return(CANCEL) ;
		break ;
	    }	/* switch err */

	    if(err == PROFOM_ERR) return(err) ;
	    if(err == DBH_ERR) return(err) ;
	}	/* for(; ; ) */
}	/* ConfirmItems() */
/*-----------------------------------------------------------------------*/
/*                                                                       */
/* Changing fields. Accept fld of the student to be changed and read     */
/* that fld 		 */
ChangeFields()
{
	int retval;
	int day;

	/* make copy screen every time field changed in case user */
	/* presses ESC-F */
	scpy((char*)&image,(char*)&s_sth, sizeof(s_sth));

	SetDupBuffers(ITEM_ST_FLD, END_FLD - 200, 2);

	/* Get The Field to Be Modified */
#ifdef	ENGLISH
	strcpy(s_sth.s_mesg,"Enter RETURN to Terminate Edit");
#else
	strcpy(s_sth.s_mesg,"Appuyer sur RETURN pour terminer l'ajustement");
#endif
	DispMesgFld((char *)&s_sth); ;
        
     	for (; ;) {
		s_sth.s_field = LV_SHORT;
		retval = ReadFields((char *)&s_sth,CHG_FLD,CHG_FLD,
			(int (*)())NULL,(int (*)())NULL, 1);

		if (retval < 0) return(-1);
		if (retval == RET_USER_ESC) break;	/* User enter ESC-F */

       		if (s_sth.s_field == 0 ) break;  /* Finished changing fields */

		if(strcmp(CurPage->Items[s_sth.s_field-1].s_status,INACTIVE)==0){
			fomer("Item Has a Deleted Status Cannot Edit");
			continue;
		}
		retval = ReadItem(s_sth.s_field - 1,UPDATE);
		if(retval != NOERROR && retval != RET_USER_ESC) {
			return(retval);
		}

		if(retval == RET_USER_ESC)
			continue;

		/* make copy screen every time field changed in case user */
		/* presses ESC-F */
		scpy((char*)&image,(char*)&s_sth, sizeof(s_sth));

		scpy((char*)&(CurPage->Items[s_sth.s_field -1]), 
		     (char*)&(s_sth.s_items[s_sth.s_field -1]),sizeof(S_item)) ;

		if(s_sth.s_fn[0] == CHANGE) {
			CurPage->I_Status[s_sth.s_field -1][0] = CHANGE;
		}
	}

     	s_sth.s_field = HV_SHORT ;
	if ( WriteFields((char *)&s_sth,CHG_FLD, CHG_FLD) < 0 ) return(-1);

     	s_sth.s_mesg[0] = HV_CHAR ;
     	DispMesgFld((char *)&s_sth);

	if(SetDupBuffers(ITEM_ST_FLD, END_FLD - 200, 0)<0) return(PROFOM_ERR);

	return(NOERROR);
}	/* ChangeFields() */
/*------------------------------------------------------------------------*/
ChangeStatus(status)
int	status;
{
	int	retval;
	int	st_fld, end_fld;

	/* Get The Field to Be Modified */
#ifdef	ENGLISH
	strcpy(s_sth.s_mesg,"Enter RETURN to Terminate Edit");
#else
	strcpy(s_sth.s_mesg,"Appuyer sur RETURN pour terminer l'ajustement");
#endif
	DispMesgFld((char *)&s_sth); ;
        
     	for (; ;) {
		s_sth.s_field = LV_SHORT;
		retval = ReadFields((char *)&s_sth,CHG_FLD,CHG_FLD,
			(int (*)())NULL,(int (*)())NULL, 1);

		if (retval < 0) return(-1);
		if (retval == RET_USER_ESC) break;	/* User enter ESC-F */

       		if (s_sth.s_field == 0 ) break;  /* Finished changing fields */

	
		st_fld  = ITEM_ST_FLD + (STEP * (s_sth.s_field-1)) + STATUS_FLD;
		end_fld  = ITEM_ST_FLD + (STEP * (s_sth.s_field-1))+STATUS_FLD;

		if(status == DELITEM) {
			if(strcmp(s_sth.s_items[s_sth.s_field-1].s_status,
			   INACTIVE)==0) {
				fomer("Item is Already Deleted");
			}
			else {
				strcpy(s_sth.s_items[s_sth.s_field-1].s_status,
			   		INACTIVE);
			}
		}
		else {
			if(strcmp(s_sth.s_items[s_sth.s_field-1].s_status,
			   ACTIVE)==0) {
				fomer("Item is Already Active");
			}
			else {
				strcpy(s_sth.s_items[s_sth.s_field-1].s_status,
			   		ACTIVE);
			}
		}
		/* Update Linked List */
		scpy((char*)&(CurPage->Items[s_sth.s_field -1]), 
		     (char*)&(s_sth.s_items[s_sth.s_field -1]),sizeof(S_item)) ;
		
		ret(WriteFields((char *)&s_sth,st_fld,end_fld));
	}
     	s_sth.s_field = HV_SHORT ;
	if ( WriteFields((char *)&s_sth,CHG_FLD, CHG_FLD) < 0 ) return(-1);

	return(NOERROR);
}
/*------------------------------------------------------------------------*/
/* Move High values to all data fields and clear the screen */

ClearScreen()
{
	int	i;

	/* Move High Values to Hedaer part */
	s_sth.s_page = HV_SHORT;
	s_sth.s_stat[0] = HV_CHAR;
	
	/* Move High Values to The one item */
	for( i=0; i < PAGESIZE ; i++) {
		InitItem(HV_CHAR,HV_DOUBLE,i);
	}

	ret( WriteFields((char *)&s_sth,PAGE_FLD, (END_FLD - 200)) );

	return(NOERROR);
}	/* ClearScreen() */
/*-------------------------------------------------------------------------*/
/* Initialize Given screen item with either Low values or High values */

InitItem(t_char, t_double, item_no)
char	t_char ;
double	t_double ;
short	item_no ;
{
	int	i;

/*	if(s_sth.s_fn[0] != CHANGE) { */
		s_sth.s_items[item_no].s_emp[0] = t_char ;
		s_sth.s_items[item_no].s_code[0] = t_char ;
		s_sth.s_items[item_no].s_hours = t_double;
		s_sth.s_items[item_no].s_teach[0] = t_char ;
/*	}
	else { 
		if( t_char != LV_CHAR) 
			s_sth.s_items[item_no].s_emp[0] = t_char ;
			s_sth.s_items[item_no].s_code[0] = t_char ;
			s_sth.s_items[item_no].s_hours = t_double;
			s_sth.s_items[item_no].s_teach[0] = t_char ;
	} */
	s_sth.s_items[item_no].s_name[0] = t_char ;
	s_sth.s_items[item_no].s_desc[0] = t_char ;
	s_sth.s_items[item_no].s_tname[0] = t_char ;
	if(t_char == HV_CHAR) { 
		s_sth.s_items[item_no].s_status[0] = t_char ;
	}

	return(NOERROR) ;
}	/* Inititem() */
/*-------------------- E n d   O f   P r o g r a m ---------------------*/
