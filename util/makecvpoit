
I=$(HOME)/include
E=$(HOME)/bin
LIBS=$(HOME)/dbh/libbfs.a /lib/libuisam.a
OBJECTS=convpoitm.o
CC=cc

$E/convpoitm.out : $(OBJECTS) $(LIBS)
	$(CC) $(OBJECTS) $(LIBS) -o  $E/convpoitm.out

.c.o :
	$(CC) -c -I$I -DENGLISH $*.c

$(OBJECTS) :convpoitm.c $I/bfs_defs.h $I/bfs_recs.h

